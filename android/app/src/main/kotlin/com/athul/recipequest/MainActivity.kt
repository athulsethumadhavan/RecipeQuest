package com.athul.recipequest

import io.flutter.embedding.android.FlutterActivity

/**
 * MainActivity is intentionally minimal — Flutter handles all UI.
 * Add platform channel methods here only if you need native Android features
 * that no Flutter plugin covers (e.g., custom biometric prompts, NFC, etc.).
 */
class MainActivity : FlutterActivity()
